services:
  tts:
    build: .
    volumes:
      - ./tts_app:/app/tts_app:ro
      - ./data/input:/data/input:ro
      - ./data/chunks:/data/chunks
      - ./data/output:/data/output
      - ./data/samples:/data/samples
      - ./models/torch:/root/.cache/torch/hub
      - ./models/tts:/root/.local/share/tts
    working_dir: /app
